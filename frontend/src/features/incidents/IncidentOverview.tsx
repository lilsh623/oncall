const labels: Record<string, string> = { status: "处理状态", project_id: "项目", environment: "环境", service: "服务", summary: "告警摘要", opened_at: "开始时间", resolved_at: "恢复时间", updated_at: "最后更新" };
export function IncidentOverview({ overview }: { overview: Record<string, string | null> }) { const visible = Object.entries(overview).filter(([key]) => labels[key]); return <section><div className="section-heading"><div><span className="eyebrow">CONTEXT</span><h2>事件概览</h2></div></div><dl className="facts">{visible.map(([key, value]) => <div key={key}><dt>{labels[key]}</dt><dd>{key.endsWith("_at") && value ? new Date(value).toLocaleString() : value ?? "—"}</dd></div>)}</dl></section>; }
