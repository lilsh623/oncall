export function EvidencePanel({ evidence, hypotheses }: { evidence: Record<string, unknown>[]; hypotheses: Record<string, unknown>[] }) { return <section><h2>证据与假设</h2><ul>{evidence.map((item) => <li key={String(item.id)}><strong>{String(item.source_ref)}</strong>：{String(item.observation)}</li>)}</ul><h3>假设</h3><ul>{hypotheses.map((item) => <li key={String(item.id)}>{String(item.description)}（置信度 {String(item.confidence ?? "—")}）</li>)}</ul></section>; }
